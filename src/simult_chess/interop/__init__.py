"""OpenSpiel interop layer (Phase 12, docs/DEVELOPMENT_addendum_v1.1.md).

Requires the optional `openspiel` extra (`open_spiel`, `numpy`). Nothing
here is imported by `simult_chess.core`, `simult_chess.rules`, or
`simult_chess.referee` -- the referee/Φ stay free of the dependency.
"""

from __future__ import annotations
